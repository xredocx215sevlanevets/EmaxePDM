<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marlonheraclito.emaxepdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DefinicoesActivity"></activity>
        <activity android:name=".AtualizarActivity" />
        <activity
            android:name=".DesistiuActivity"
            android:parentActivityName=".ExameActivity" />
        <activity
            android:name=".FaltouActivity"
            android:parentActivityName=".ExameActivity" />
        <activity
            android:name=".PresenteActivity"
            android:parentActivityName=".ExameActivity" />
        <activity android:name=".NovoActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ExameActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>